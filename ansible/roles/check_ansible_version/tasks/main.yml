---

- name: Check if Ansible meets version requirements.
  ansible.builtin.assert:
    that: "ansible_version.full is version_compare('{{ ansible_min_version }}', '>=')"
    msg: >
      "You must update Ansible to at least {{ ansible_min_version }} to use this ansible collection."
  vars:
    ansible_min_version: 2.9
