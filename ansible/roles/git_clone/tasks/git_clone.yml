---
- name: Ensure repo dest directory exists
  file:
    path: "{{ git_repo_dest }}"
    state: directory

- name: Perform the git clone
  git: 
    repo: "{{ git_repo }}"
    version: "{{ git_version }}"
    dest: "{{ git_repo_dest }}"
    accept_hostkey: yes
