---

- name: Apply MCSP product registration
  kubernetes.core.k8s:
    api_key: "{{ hostvars['cluster-facts']['api_key'] | default(omit) }}"
    kubeconfig: "{{ hostvars['cluster-facts']['kubeconfig_path'] }}"
    template: product-registration.yaml.j2
  register: _result
