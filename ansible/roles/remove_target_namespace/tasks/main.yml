---

- name: Remove target_namespace
  kubernetes.core.k8s:
    api_key: "{{ api_key | default(omit) }}"
    kubeconfig: "{{ kubeconfig_path }}"
    kind: Namespace
    name: "{{ target_namespace }}"
    state: absent
    wait: true
  register: _result
