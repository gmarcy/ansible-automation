---

all:
  children:
    localhost_facts:
      hosts: localhost-facts
      vars:
        ansible_host: localhost
        ansible_connection: local
    playbook_secrets:
      hosts: playbook-secrets
      vars:
        ansible_host: localhost
        ansible_connection: local
    ipxe_setup:
      hosts: ipxe-setup
      vars:
        ansible_host: localhost
        ansible_connection: local
    ipmi_control:
      hosts: ipmi-control
      vars:
        ansible_host: localhost
        ansible_connection: local
    assisted_install:
      hosts: assisted-install
      vars:
        ansible_host: localhost
        ansible_connection: local
    cluster_facts:
      hosts: cluster-facts
      vars:
        ansible_host: localhost
        ansible_connection: local
    cluster_install:
      hosts: cluster-install
      vars:
        ansible_host: localhost
        ansible_connection: local
    cluster_storage:
      hosts: cluster-storage
      vars:
        ansible_host: localhost
        ansible_connection: local
