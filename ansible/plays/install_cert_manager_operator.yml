---

- name: Install Community Cert Manager
  hosts: cluster_operators
  gather_facts: false
  tags: cert_manager
  roles:
  - role: install_cert_manager
    api_key: "{{ hostvars['cluster-facts']['api_key'] | default(omit) }}"
    kubeconfig_path: "{{ hostvars['cluster-facts']['kubeconfig_path'] }}"
