---

- name: Print ODLM cluster information
  hosts: cluster_facts
  gather_facts: false
  tags: cluster_info
  roles:
  - role: print_ocp_cluster_info
    description: "Operand Deployment Lifecycle Manager"
