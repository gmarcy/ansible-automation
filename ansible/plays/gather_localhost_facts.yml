---

- name: Create localhost_facts localhost group for this play
  hosts: localhost
  gather_facts: false
  tags: localhost_facts
  roles:
  - role: create_localhost_groups
    localhost_groups:
    - host_name: localhost-facts
      group_name: localhost_facts

- name: Gather localhost facts
  hosts: localhost_facts
  gather_facts: false
  tags: localhost_facts
  roles:
  - role: gather_localhost_facts
