---

- name: Install openshift on the cluster host using assisted-install
  hosts: cluster_install
  gather_facts: false
  tags: cluster_install
  roles:
  - role: install_cluster
    cluster_dir: "{{ hostvars['cluster-facts']['cluster_dir'] }}"
