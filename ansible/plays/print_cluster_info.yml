---

- name: Print cluster information
  hosts: cluster_facts
  gather_facts: false
  tags: cluster_info
  roles:
  - role: check_playbook_terminated
  - role: print_cluster_info
    description: "{{ cluster_description }}"
