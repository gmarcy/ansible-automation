---

w3id_oauth_issuer_url: "{{ secrets_dir + '/w3id_oauth_issuer_url' }}"
w3id_oauth_issuer_client_id: "{{ secrets_dir + '/w3id_oauth_issuer_client_id' }}"
w3id_oauth_issuer_client_secret: "{{ secrets_dir + '/w3id_oauth_issuer_client_secret' }}"
