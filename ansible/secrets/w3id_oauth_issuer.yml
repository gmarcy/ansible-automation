---

w3id_oauth_issuer_url: "{{ lookup('unvault', secrets_dir + '/w3id_oauth_issuer_url') | trim }}"
w3id_oauth_issuer_client_id: "{{ lookup('unvault', secrets_dir + '/w3id_oauth_issuer_client_id') | trim }}"
w3id_oauth_issuer_client_secret: "{{ lookup('unvault', secrets_dir + '/w3id_oauth_issuer_client_secret') | trim }}"
