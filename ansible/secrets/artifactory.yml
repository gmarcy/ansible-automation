---

artifactory_fqdn: "{{ secrets_dir + '/artifactory_fqdn' }}"
artifactory_user: "{{ secrets_dir + '/artifactory_user' }}"
artifactory_identity_token: "{{ secrets_dir + '/artifactory_identity_token' }}"
