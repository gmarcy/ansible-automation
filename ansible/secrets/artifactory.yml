---

artifactory_fqdn: "{{ lookup('unvault', secrets_dir + '/artifactory_fqdn') | trim }}"
artifactory_user: "{{ lookup('unvault', secrets_dir + '/artifactory_user') | trim }}"
artifactory_identity_token: "{{ lookup('unvault', secrets_dir + '/artifactory_identity_token') | trim }}"
