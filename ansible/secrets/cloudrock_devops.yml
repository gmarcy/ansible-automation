---

cloudrock_devops_user: "{{ lookup('unvault', secrets_dir + '/cloudrock_devops_user') | trim }}"
cloudrock_devops_apikey: "{{ lookup('unvault', secrets_dir + '/cloudrock_devops_apikey') | trim }}"
