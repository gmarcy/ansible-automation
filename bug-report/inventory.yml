---

all:
  hosts:
    localhost:
      prefix_url: "prefix"
      inv_value_fact: "{{ hostvars['localhost'].prefix_url + '/special-value' }}"
      inv_value: "{{ inv_value_fact }}"
  vars:
    actual_value: "{{ inv_value }}"
    indirect_value: "{{ hostvars['localhost'].inv_value }}"
