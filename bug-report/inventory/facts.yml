---

all:
  children:
    fact_group:
      hosts:
        fact-group:
      vars:
        prefix_url: 'prefix'
    test_group:
      hosts:
        test-group:
      vars:
        inv_value_fact: "{{ hostvars['fact-group']['prefix_url'] + '/special-value' }}"
